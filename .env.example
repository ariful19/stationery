# Stationery workspace environment defaults
# Copy this file to .env and tweak values that differ from your setup.

# --- API server -------------------------------------------------------------
PORT=8080
# Absolute or relative path to the SQLite database. Takes precedence over DB_PATH.
DATABASE_URL=./stationery.sqlite
# Optional explicit path used by better-sqlite3 (overrides DATABASE_URL when set).
DB_PATH=

# --- Documentation ----------------------------------------------------------
# Host used when generating links to the Swagger UI.
PUBLIC_API_URL=http://localhost:8080

# --- Puppeteer / PDF rendering ---------------------------------------------
# Accepts: true | false | 0 | 1 | shell. Use 0/false to open Chromium windows locally.
PUPPETEER_HEADLESS=true
# Path to a Chromium/Chrome executable (required in minimal Docker images).
PUPPETEER_EXECUTABLE_PATH=
# Directory for saving ad-hoc PDF previews generated by the API.
PDF_PREVIEW_DIR=./tmp/previews
# Flip to true to return mock buffers without launching Chromium (handy for tests).
MOCK_INVOICE_PDF=false
MOCK_REPORT_PDF=false

# --- Invoice configuration --------------------------------------------------
BILLING_ROUNDING_MODE=HALF_EVEN
INVOICE_PREFIX=INV
# Optional number of digits to pad invoice sequences with (e.g. 4 => 0001).
INVOICE_SEQUENCE_PADDING=

# --- Testing & tooling ------------------------------------------------------
# Isolated database used by Playwright suites.
PLAYWRIGHT_DATABASE_URL=./tmp/playwright-api.sqlite

# --- Faker seeding knobs ----------------------------------------------------
# Set to true to wipe existing records before Faker inserts.
SEED_RESET=false
# Faker record counts (overridden by CLI args/env vars).
SEED_CUSTOMERS=20
SEED_PRODUCTS=15
SEED_INVOICES=35
# Optional deterministic seed for the Faker RNG.
SEED_FAKER_SEED=
